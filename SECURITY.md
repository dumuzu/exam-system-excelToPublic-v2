# 安全说明

请不要在公开 Issue、日志或截图中提交数据库连接串、会话密钥、管理员密码、学生名册或真实考试数据。

如发现安全问题，请通过 GitHub Security Advisory 的私密报告入口联系仓库维护者。在问题修复并发布前，请不要公开漏洞细节。

部署者需要自行负责：

- 为 `SESSION_SECRET` 与 `CRON_SECRET` 生成不同的高强度随机值。
- 使用独立的 PostgreSQL 数据库，并限制数据库账号权限。
- 只在受控终端中使用 `MIGRATION_DATABASE_URL` 和管理员初始化密码。
- 保持 Node.js、依赖和数据库补丁处于受支持版本。
- 在正式考试前完成备份、恢复、容量与网络演练。
